Add a new schedule:
  parameters:
    - in: body
      name: body
      schema:
        id: Schedule
        required:
          - doctor_id
          - patient_id
          - appointment_date
